---
import Layout from "@/layouts/Layout.astro"
import { Image } from "astro:assets"

import howo_img from "@/assets/images/howo.png"
import { cta, services, uslugi } from "@/data/informations"
import { Icon } from "astro-icon/components"
---

<Layout title="" description="">
	<div class="mx-auto flex flex-col gap-6 text-center">
		<h1 class="md:text-6xl text-5xl lg:text-8xl font-black tracking-tight">BIG HOWO</h1>
		<span class="text-xl font-medium">НАДЕЖНОСТЬ. КАЧЕСТВО. СКОРОСТЬ.</span>
	</div>
	<div class="bg-white p-8 rounded-xl">
		<p class="text-xl text-zinc-600 text-center leading-relaxed">
			Мы предоставляем профессиональные услуги по перевозке сыпучих материалов и
			строительных грузов. Надёжность, оперативность и качественный сервис — наши
			приоритеты!
		</p>
	</div>
	<div class="bg-white rounded-xl">
		<div class="flex flex-col md:flex-row">
			<div class="md:p-8 p-6 flex flex-col gap-8">
				<p class="text-xl md:text-left text-center text-zinc-600 leading-relaxed">
					Мы используем современные самосвалы <b>HOWO</b>, обеспечивающие высокую
					грузоподъёмность и эффективность перевозок.
				</p>
				<h2 class="heading-h2 md:text-left text-center">Преимущества работы с нами:</h2>
				<div class="flex flex-wrap md:justify-start justify-center gap-4">
					{
						cta &&
							cta.map(item => (
								<p class="px-5 py-2 flex gap-2 items-center border-[0.5px] border-zinc-200 bg-zinc-50 rounded-lg">
									<Icon name={item.icon} />
									<span>{item.text}</span>
								</p>
							))
					}
				</div>
			</div>
			<Image
				class="md:w-1/3 -order-1 mx-auto object-cover rounded-xl"
				src={howo_img}
				alt="HOWO Самосвал"
				format="webp"
			/>
		</div>
	</div>

	<div class="p-8 bg-white flex flex-col gap-8 rounded-xl w-full" id="services">
		<h2 class="heading-h2">Наши услуги:</h2>
		<div class="flex gap-6 flex-wrap">
			{
				uslugi &&
					uslugi.map(item => (
						<div class="flex gap-3 items-center">
							<div class="relative">
								<div class="w-2 h-2 rounded-full bg-green-400 animate-ping" />
								<div class="w-2 h-2 rounded-full bg-green-400 absolute inset-0" />
							</div>
							<p class="font-medium">{item.text}</p>
						</div>
					))
			}
		</div>
	</div>
	<div class="p-8 bg-white flex flex-wrap gap-8 rounded-xl w-full">
		{
			services &&
				services.map(item => (
					<p class="px-5 py-2 flex gap-2 items-center border-[0.5px] border-zinc-200 bg-zinc-50 rounded-lg">
						<span class="font-medium text-lg">{item.service}</span>
						<span class="text-green-600">{item.price}</span>
					</p>
				))
		}
	</div>
</Layout>
